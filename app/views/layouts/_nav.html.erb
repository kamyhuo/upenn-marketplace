
<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
  <div class="container">
  
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>


  <div class="collapse navbar-collapse" id="collapsibleNavbar">
    <ul class="nav">
      <li class="nav-item"><span class="badge badge-pill badge-default">
        <%= link_to 'Home', '/', {:style=>'color:#FFFFFF;', :class => "css_class"} %>  </span>   |
      </li>
      <li class="nav-item"><span class="badge badge-pill badge-default">
        <%= link_to 'All Shops', '/shops', {:style=>'color:#FFFFFF;', :class => "css_class"} %> </span>   |
      </li>
      <li class="nav-item"><li class="nav-item"><span class="badge badge-pill badge-default">
        <%= link_to 'Add Shop', '/shops/new', {:style=>'color:#FFFFFF;', :class => "css_class"} %> </span>  
      </li>  
      <%= yield %>   
    </ul>
  </div>  


    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">


        <%# Only display the current user's name, friend requests and log out links if a user is logged in %>
        <% if logged_in? %>
          <li class="nav-item"><%= link_to current_user.username, current_user, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'Log out', '/logout', method: :delete, class: 'nav-link' %></li>

        <% end %>

        <%# Only display the sign up and log in links if no user is logged in %>
        <% if !logged_in? %>
          <li class="nav-item"><%= link_to 'Sign up', new_user_path, class: 'nav-link' %></li>
          <li class="nav-item"><%= link_to 'Log in', '/login', class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>